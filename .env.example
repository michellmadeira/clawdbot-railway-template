# Research pipeline — banco de estado (get/set/list por run_id e stage).
# Definir RESEARCH_DB_URL no painel do Railway (Variáveis do container), não no GitHub.
# Exemplo: RESEARCH_DB_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE
